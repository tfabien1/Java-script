//const= values that does not change
let pi = 3.1456;
let rad;
let circumference;
rad = window.prompt("What is the radiaus to find the circumfrence ?");
rad = Number(rad);
circumference = 2 * pi * rad;
console.log(circumference);